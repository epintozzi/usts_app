<div class="container">
  <h2 class="page-title"> <%= @gallery.name %> </h2>

  <% @gallery.photos.each do |photo| %>
    <div class="col-md-4">
      <%= link_to(image_tag(photo.image.url(:thumb), class: "gallery-thumb"), photo_path(photo)) %>
    </div>
  <% end %>

</div>
